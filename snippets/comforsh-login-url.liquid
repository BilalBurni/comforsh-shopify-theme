{%- comment -%}
  New Customer Accounts login URL (this store has no Classic option).
  Login + account creation share the same Shopify-hosted auth flow.
{%- endcomment -%}
{{- routes.storefront_login_url -}}
